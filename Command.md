# 名前
* .. 親ディレクトリ。ひとつ前のディレクトリに戻る。
* .　カレントディレクトリ。現在のフォルダのこと。

# vim


# 権限
* 種類
  * 4 / r　読み込み可能
  * 2 / w　書き込み可能
  * 1 / x　実行可能（ディレクトリの探索可能）
  * 0 / -　何も許可しない
* 例
  * 7　読み込み/書き込み/実行可能　（4 + 2 + 1）
  * 6 　読み込み/書き込み可能　（4 + 2）


# systemctl

# その他
* echo 文字を表示するコマンド。スクリプトなど使う。
 * echo "aa" aaと表示される。
 * echo "aa" >> ii.txt　ii.txtにaaと書き込む。
* wget インターネット上からファイルを入手するコマンド
  * wget <URL> でファイルを入手することができる

# ファイル
* touch ファイルのタイムスタンプを更新するコマンド。ファイルが存在しない場合は新規作成される。
  * touch aa.txt　ファイルが存在する場合は、aa.txtのタイムスタンプが更新される。存在しない場合は、新規作成される。
* cat ファイルの中身を表示するコマンド
 * cat aa.txt　aa.txtの中身が表示される。
* pwd カレントディレクトリを表示するコマンド
* cd　ディレクトリを移動するコマンド
  * cd ..　親ディレクトリに戻るコマンド
  * cd ~　ホームディレクトリに戻るコマンド
* rm　ファイルを削除するコマンド
* rmdir ディレクトリを削除するコマンド
* cp　ファイルをコピーするコマンド
* ls ディレクトリの中身を表示するコマンド
  * ls -l ディレクトリを詳細表示（種類・権限・所有者・サイズを表示する）するコマンド
* mkdir ディレクトリを作成するコマンド

